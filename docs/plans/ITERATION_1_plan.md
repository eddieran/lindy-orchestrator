我希望你针对所有命令进行一轮优化，我提一些问题。

1. status和logs可以合并一下，一起输出最近的一些重要情况，包括status和logs
2. init和scaffold以及onboard可以合并一下，形成统一的onboard命令，可以根据项目当前的情况(需要精确判断，空项目，已有的项目)，来交互式文档，理解项目本身，或者用户提出诉求，根据所有的上下文，去初始化项目，形成可编排的必要架构和分组。
如果项目之前已经onboard过了，可以根据用户的诉求，或者当前项目的情况，进行交互式的重新onboard或者优化
onboard要先交互式的输出plan，获得认可之后再执行。
3. mailbox可以默认开启吗？感觉是一个很强力的命令
4. 你需要重新去设计 run命令背后的console的展示方式，你现在verbose的时候，展示的内容太多了，根本不可读，一页展示不全，你可以参考一下elixir的设计，把task的dag通过asscii的方式画出来，然后在上面去实时的标注当前的进度，然后每一个进度旁边多一个小小的对话框，更新对应的reasoning或者日志，要有可见性。
5. 所有的这些，你需要完整的测试，构建完整的自动化测试链条，端到端测试，cicd
6. 全部完成，端到端测试也完成后，推pr，cicd通过，merge，release版本，更新本地 
